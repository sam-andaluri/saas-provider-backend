apiVersion: apps/v1
kind: Deployment
metadata:
  name: saas-provider-backend-deployment
  namespace: saas-provider-backend
spec:
  template:
    spec:
      containers:
      - image: sandaluri/saas-provider-backend:latest
        name: saas-provider-backend
